#!/bin/sh

# Block AI attribution
if grep -qi "co-authored-by\|co-authored by\|generated with\|noreply@anthropic" "$1"; then
  echo "error: commit message contains AI attribution — remove it before committing"
  exit 1
fi

bun x commitlint --edit "$1"
